"use client";
import React from "react";

const NemLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    // 网元管理模板
    <div>
      {children}网元管理模板
    </div>
  );
};

export default NemLayout;
